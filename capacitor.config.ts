import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thijsvndmeer.clashcounter',
  appName: 'ClashCounter',
  webDir: 'dist'
};

export default config;
